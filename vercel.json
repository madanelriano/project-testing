{
  "version": 2,
  "builds": [
    {
      "src": "src/package.json",
      "use": "@vercel/next",
      "config": { "zeroConfig": true }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/$1" }
  ]
}